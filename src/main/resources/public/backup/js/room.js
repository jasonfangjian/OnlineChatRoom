'use strict';

let info;

window.onload = function () {
    info = JSON.parse(localStorage.getItem("info"));

    // TODO: ask controller for the chat room info and messages
    // TODO: check if the user is banned (if so, alert("You're banned") and return to Main page)

    // TODO: disable ban function if the user is not admin
}
